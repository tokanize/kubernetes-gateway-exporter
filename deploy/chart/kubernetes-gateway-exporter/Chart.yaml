apiVersion: v2
name: kubernetes-gateway-exporter
description: A Helm chart for deploying the Kubernetes Gateway Exporter
type: application
version: 0.1.0
# appVersion matches the release tag scheme without the leading "v" so that
# the default image tag (used when values.image.tag is empty) resolves to
# ghcr.io/tokanize/kubernetes-gateway-exporter:0.1.0 — consistent with GHCR.
appVersion: "0.1.0"
